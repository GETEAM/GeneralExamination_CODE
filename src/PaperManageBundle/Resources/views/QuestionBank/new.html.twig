{% extends '::base.html.twig' %}

{% block body -%}
    <h1>QuestionBank creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('questionbank') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
